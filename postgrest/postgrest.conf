# SECURITY (2026-08-11): the jwt-secret that used to be hardcoded here was
# committed to git in plaintext — treat it as permanently leaked. Do not put
# a real secret back in this file. On the VPS, override via the PGRST_JWT_SECRET
# environment variable instead (PostgREST reads PGRST_<UPPERCASED_KEY> env vars
# and they take precedence over this file), or gitignore a VPS-local copy of
# this file the same way docker-compose.dokploy.yml's secrets are handled.
db-uri = "postgresql://postgres:CHANGE_ME@172.18.0.5:5432/postgres"
db-schemas = "easylegal"
db-anon-role = "postgrest_anonymous"
db-extra-search-path = "public"
server-port = 3001
server-host = "0.0.0.0"
jwt-secret = "CHANGE_ME_SET_VIA_PGRST_JWT_SECRET_ENV_VAR"
secret-is-base64 = false
