{
  "name": "easylegal-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test:docker-context": "node scripts/check-docker-context-imports.mjs",
    "test:wa-inventory": "node scripts/check-whatsapp-cta.mjs"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
